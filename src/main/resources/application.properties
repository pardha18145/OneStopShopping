spring.datasource.url=jdbc:postgresql://localhost:5433/onestopshopping
spring.datasource.username=postgres
spring.datasource.password=12345678
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Spring Security Queries
security.query.user=SELECT username,password,enabled FROM users WHERE username = ?
security.query.authorities=SELECT username,role FROM users WHERE username = ? 

#spring.security.user.name=pardhu
#spring.security.user.password=pardhu